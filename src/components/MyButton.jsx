import "../css/MyButton.css"


// React component names must always start with a capital letter, while HTML tags must be lowercase.
export default function MyButton() {
  return (
    <button className="myButton">Click Me Button</button>
  )
}